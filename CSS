<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Music Player UI</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', sans-serif;
    }

    body {
      background: linear-gradient(135deg, #1d2671, #c33764);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .player {
      width: 320px;
      background: #111;
      border-radius: 20px;
      padding: 20px;
      color: #fff;
      box-shadow: 0 20px 40px rgba(0,0,0,0.5);
    }

    .album-art {
      width: 100%;
      height: 260px;
      background: linear-gradient(135deg, #ff512f, #dd2476);
      border-radius: 15px;
      margin-bottom: 15px;
      position: relative;
      overflow: hidden;
    }

    .album-art::after {
      content: "♪";
      font-size: 120px;
      color: rgba(255,255,255,0.25);
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    .song-info {
      text-align: center;
      margin-bottom: 15px;
    }

    .song-info h3 {
      font-size: 1.1rem;
    }

    .song-info p {
      font-size: 0.9rem;
      color: #bbb;
    }

    .progress-container {
      background: #333;
      height: 6px;
      border-radius: 10px;
      cursor: pointer;
      margin: 15px 0;
      overflow: hidden;
    }

    .progress {
      width: 45%;
      height: 100%;
      background: #ff4c68;
      transition: width 0.3s ease;
    }

    .controls {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 15px;
    }

    .btn {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background: #222;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .btn:hover {
      background: #ff4c68;
      transform: scale(1.1);
    }

    .play {
      width: 65px;
      height: 65px;
      background: #ff4c68;
    }

    .play:hover {
      background: #ff2f55;
    }

    .btn span {
      font-size: 1.2rem;
    }

    .play span {
      font-size: 1.6rem;
    }

    .volume {
      margin-top: 15px;
    }

    .volume-bar {
      width: 100%;
      height: 5px;
      background: #333;
      border-radius: 10px;
      overflow: hidden;
      cursor: pointer;
    }

    .volume-level {
      width: 70%;
      height: 100%;
      background: #00e5ff;
      transition: width 0.3s ease;
    }

    /* Interactive illusion */
    .player:hover .progress {
      width: 70%;
    }

    .player:hover .volume-level {
      width: 90%;
    }

  </style>
</head>
<body>

  <div class="player">
    <div class="album-art"></div>

    <div class="song-info">
      <h3>Dreamscape</h3>
      <p>Unknown Artist</p>
    </div>

    <div class="progress-container">
      <div class="progress"></div>
    </div>

    <div class="controls">
      <div class="btn"><span>⏮</span></div>
      <div class="btn play"><span>▶</span></div>
      <div class="btn"><span>⏭</span></div>
    </div>

    <div class="volume">
      <p style="font-size:0.8rem; margin-bottom:5px;">Volume</p>
      <div class="volume-bar">
        <div class="volume-level"></div>
      </div>
    </div>
  </div>

</body>
</html>
